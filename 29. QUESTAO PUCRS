#include <stdio.h>

int main() {
    int soma = 0, cont = 0;

    for (int num = 13; num <= 73; num++) {
        soma += num;
        cont++;
    }

    printf("Media: %.2f\n", (float)soma / cont);

    return 0;
}
